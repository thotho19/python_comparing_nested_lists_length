data = [['a','b','c'],
        ['a','b']
        ]


lenSize = len(data[0])
comparingState = True

for d in data:
    if lenSize != len(d):
        same = False
    else:
        pass
print(comparingState)
